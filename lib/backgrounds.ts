/** "Plano Azul Base" — the starry dark blue/purple backdrop (from Figma),
 * used as a full-bleed background for dark CTA/banner blocks. Reuse this one
 * asset (via next/image `fill` + `object-cover`) wherever another page needs
 * the same treatment, instead of re-exporting a new image from Figma. */
export const PLANO_AZUL_BASE = "/images/plano-azul-base.png";
