/** Icons only — label/description/badge text lives in messages/<locale>/legal.json's
 *  `certifications.items`, same order. Mirrors a subset of seguranca.json's own
 *  compliance section (see config/seguranca-page.ts) so both pages stay visually
 *  consistent when referencing the same certifications. */
export const legalComplianceIcons: string[] = [
  "/icons/seguranca/iso.svg",
  "/icons/seguranca/soc2.svg",
  "/icons/seguranca/lgpd-brasil.svg",
  "/icons/seguranca/gdpr.svg",
  "/icons/seguranca/hipaa.svg",
];
