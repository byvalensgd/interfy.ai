/** Icons only — label text lives in messages/<locale>/platform.json's `hero.chips`, same order. */
export const platformHeroChipIcons: string[] = [
  "/icons/platform/ai-native.svg",
  "/icons/platform/cloud-nativa.svg",
  "/icons/platform/100-web.svg",
  "/icons/platform/web-mobile.svg",
  "/icons/platform/idiomas.svg",
  "/icons/platform/paises.svg",
];

/** Icons only — title/description text lives in messages/<locale>/platform.json's `aiGrid.items`, same order. */
export const platformAiGridIcons: string[] = [
  "/icons/platform/ai-busca.svg",
  "/icons/platform/ai-processos.svg",
  "/icons/platform/ai-extracao.svg",
  "/icons/platform/ai-agentes.svg",
  "/icons/platform/ai-classificacao.svg",
  "/icons/platform/ai-formularios.svg",
  "/icons/platform/ai-aprovacoes.svg",
  "/icons/platform/ai-voz.svg",
];

/** Icons only — title/description text lives in messages/<locale>/platform.json's `workspace.items`, same order. */
export const platformWorkspaceIcons: string[] = [
  "/icons/platform/workspace-unificado.svg",
  "/icons/platform/workspace-exclusivo.svg",
  "/icons/platform/workspace-automacoes.svg",
  "/icons/platform/workspace-controle.svg",
];

/** Icons only — title/checklist text lives in messages/<locale>/platform.json's `trust.cards`, same order. */
export const platformTrustCardIcons: string[] = [
  "/icons/platform/trust-seguranca.svg",
  "/icons/platform/trust-apis.svg",
  "/icons/platform/trust-infra.svg",
];

/** Icons only — label/sublabel text lives in messages/<locale>/platform.json's `cta.features`, same order. */
export const platformCtaFeatureIcons: string[] = [
  "/icons/platform/paises.svg",
  "/icons/platform/idiomas.svg",
  "/icons/platform/cta-regioes.svg",
  "/icons/platform/web-mobile.svg",
  "/icons/platform/cta-paises-local.svg",
  "/global/fast-support.svg",
];
