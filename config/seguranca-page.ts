/** Icons only — label/title/description text lives in messages/<locale>/seguranca.json,
 *  paired with these arrays by index — same pattern as config/platform-page.ts. */

/** Icons only — label text lives in messages/<locale>/seguranca.json's `hero.diagram.items`, same order. */
export const segurancaHeroDiagramIcons: string[] = [
  "/icons/seguranca/shield-check.svg",
  "/icons/seguranca/key.svg",
  "/icons/seguranca/clock.svg",
  "/icons/seguranca/workspace.svg",
  "/icons/seguranca/database.svg",
  "/icons/seguranca/user-check.svg",
  "/icons/seguranca/globe-compliance.svg",
  "/icons/seguranca/server.svg",
];

/** Icons only — label text lives in messages/<locale>/seguranca.json's `hero.stats`, same order. */
export const segurancaHeroStatIcons: string[] = [
  "/icons/seguranca/stat-cloud.svg",
  "/icons/seguranca/clock.svg",
  "/icons/seguranca/shield-check.svg",
];

/** Icons only — title/description text lives in messages/<locale>/seguranca.json's `controls.items`, same order. */
export const segurancaControlsIcons: string[] = [
  "/icons/seguranca/shield-check.svg",
  "/icons/seguranca/database.svg",
  "/icons/seguranca/lock.svg",
  "/icons/seguranca/user-check.svg",
  "/icons/seguranca/fingerprint.svg",
  "/icons/seguranca/ip-region.svg",
  "/icons/seguranca/doc.svg",
  "/icons/seguranca/monitor-scale.svg",
  "/icons/seguranca/cloud-security.svg",
  "/icons/seguranca/load-check.svg",
  "/icons/seguranca/storage-inbox.svg",
  "/icons/seguranca/scale-growth.svg",
];

/** Icons only — title/description/badge text lives in messages/<locale>/seguranca.json's `compliance.items`, same order. */
export const segurancaComplianceIcons: string[] = [
  "/icons/seguranca/iso.svg",
  "/icons/seguranca/hipaa.svg",
  "/icons/seguranca/lgpd-brasil.svg",
  "/icons/seguranca/gdpr.svg",
  "/icons/seguranca/soc2.svg",
  "/icons/seguranca/outros-padroes.svg",
];

/** Icons only — label text lives in messages/<locale>/seguranca.json's `architecture.flow`, same order. */
export const segurancaArchitectureFlowIcons: string[] = [
  "/icons/seguranca/user-profile.svg",
  "/icons/seguranca/cdn-globe.svg",
  "/icons/seguranca/load-balancer.svg",
];

/** Icons only — label text lives in messages/<locale>/seguranca.json's `architecture.services`, same order. */
export const segurancaArchitectureServiceIcons: string[] = [
  "/icons/seguranca/storage-inbox.svg",
  "/icons/seguranca/encryption.svg",
  "/icons/seguranca/cloudwatch.svg",
  "/icons/seguranca/cloudtrail.svg",
  "/icons/seguranca/shield-plain.svg",
];

/** Icons only — label/sublabel text lives in messages/<locale>/seguranca.json's `cta.features`, same order. */
export const segurancaCtaFeatureIcons: string[] = [
  "/icons/seguranca/shield-check.svg",
  "/icons/seguranca/eye.svg",
  "/icons/seguranca/doc-check.svg",
  "/icons/seguranca/planet-web.svg",
  "/icons/seguranca/lamp-idea.svg",
];
